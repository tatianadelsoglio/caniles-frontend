import React from 'react';
import { useNavigate } from "react-router-dom";
import "./Main.css";

const Main = () => {

    let navigate = useNavigate();

    return (
        <div>
            <h1>Main</h1>
        </div>
    );
};

export default Main;